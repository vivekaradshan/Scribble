package main.scala.lectures.part3fp

object AnonymousFunctions extends App{

  //anonymous function (LAMBDA) -> comes from lambda calculus mathematical representation of functional programming
  val doubler = (x:Int) => x*2
  val doubler_dup:Int =>Int = x => x*2
}
